# runtime artifacts (regenerated; wallpaper-*/custom-* are timestamped each set
# so macOS actually refreshes the desktop picture)
wallpaper-*.png
custom-*.png
companion-config.json
companion-state.json
custom-words.json
pet-position.json
pet-closed
wordpaper.sqlite*
.wordpaper-data/
runtime-data/

# render-test output
scripts/out_v3/

# companion one-click zip (rebuilt on demand by server /companion.zip)
scripts/每日壁纸伴侣.zip

# Local deployment secrets. Keep the documented template only.
.env
.env.*
!.env.example

# OS / editor
.DS_Store
scripts/node-dist/
.wrangler/
cloudflare-wordpaper.*.log

# DMG build artifacts (python3 scripts/package_app.py)
scripts/build/
scripts/dist/
scripts/.venv-build/
